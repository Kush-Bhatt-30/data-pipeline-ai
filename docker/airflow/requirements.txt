# Airflow runtime deps for DAG tasks (kept separate from the main app image)
boto3==1.35.99
joblib==1.4.2
kafka-python==2.0.2
pandas==2.2.3
pyarrow==18.1.0
pyyaml==6.0.2
requests==2.32.3
scikit-learn==1.6.0

# Local Spark execution inside Airflow for the demo DAG
pyspark==3.5.3
